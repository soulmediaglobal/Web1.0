import { Link } from 'react-router-dom'
export function ContentState({ kind, message }: { kind: 'loading' | 'empty' | 'error' | 'not-found'; message?: string }) {
  const copy = { loading: ['Loading', 'Published content is loading.'], empty: ['Content unavailable', 'No published content is available right now.'], error: ['Temporarily unavailable', 'We could not load published content. Please try again shortly.'], 'not-found': ['Case study not found', 'This work is unavailable or has not been published.'] }[kind]
  return <section className="relative min-h-[55vh] bg-[#0a0a0a] px-6 py-24 text-white md:px-16" aria-live="polite"><div className="mx-auto max-w-[1100px] border border-white/10 bg-[#111] p-8 md:p-12"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-[#ffb4a8]">{copy[0]}</p><h1 className="mt-4 font-['Bebas_Neue'] text-5xl uppercase tracking-wide">{copy[1]}</h1>{message && import.meta.env.DEV && <p className="mt-4 font-mono text-xs text-gray-500">{message}</p>}{kind !== 'loading' && <Link to="/" className="mt-8 inline-flex border border-[#D0190F] px-5 py-3 font-mono text-[10px] uppercase tracking-[0.14em]">Back Home</Link>}</div></section>
}
